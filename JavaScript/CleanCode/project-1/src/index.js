import { evaluateStudent } from './components/evaluateStudent.js';

const student1 = { name: 'Carlos', grades: [5, 7, 9] };
const student2 = { name: 'Julia', grades: [8, 6, 9] };

evaluateStudent(student1);
evaluateStudent(student2);
